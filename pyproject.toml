[project]
name = "setup-md"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "k5-mot", email = "34744243+k5-mot@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
setup-md = "setup_md:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
